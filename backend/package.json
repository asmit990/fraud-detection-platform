{
  "name": "fraud-detection-platform",
  "version": "1.0.0",
  "description": "Real-time Event-Driven Fraud Detection Platform with Stripe-grade Idempotency & Gemini AI",
  "scripts": {
    "simulate": "npx ts-node scripts/simulate.ts",
    "test:all": "(cd services/transaction-service && npm test) && (cd services/fraud-service && npm test) && (cd services/alert-service && npm test)",
    "build:all": "(cd services/transaction-service && npm run build) && (cd services/fraud-service && npm run build)"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  }
}
